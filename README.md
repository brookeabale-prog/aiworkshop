# Brooke Bale — Resume Website

A single-page personal resume site for Brooke Bale, built with Next.js (App Router), TypeScript, and Tailwind CSS.

## Live URL

`<LIVE_URL_PLACEHOLDER>`

## Running locally

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Build

```bash
npm run lint
npm run build
```

## Skipped / notes

- Phone number omitted (not opted in; see PRD privacy section).
- No street address published (city/state only).
- Static server-rendered single page; no CMS, analytics, or contact form (phase 2).